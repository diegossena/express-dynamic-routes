/**
 * @type {import('next').NextConfig}
 */
const nextConfig =
  module.exports = () => {
    /**
     * @type {import('next').NextConfig}
     */
    const nextConfig = {
      distDir: 'app',
      esModule: true
    }
    return nextConfig
  }

module.exports = nextConfig
